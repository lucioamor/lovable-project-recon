# Opportunity Monitor — Recon Usage Report

**Waste-score:** 28/100 · Mostly healthy — minor hygiene only.  
_ref `qjptopcdxcjtrmmxkkcn` · TanStack Start · mode `index` · evidence 2026-06-30 · scanned 2026-07-02_

---

## Capability matrix

| Capability | State |
|---|---|
| Lovable Cloud (DB) | ✅ active |
| Lovable AI | ✗ not configured |
| Tables (public) | 15 |
| Cron jobs | 0 |
| Edge functions | 22 |
| Storage buckets | 0 |
| Realtime | ✗ off |
| RLS policies seen | 0 |

## Findings

| # | Policy | Severity | Conf. | Finding | Est. saved |
|--:|---|---|---|---|---|
| 1 | POL-2 | 🟠 High | confirmed | Unbounded log/raw table `job_snapshots` (96 MB, 76% of DB) | 96 MB |

### 1. [POL-2] Unbounded log/raw table `job_snapshots` (96 MB, 76% of DB)

🟠 High · confidence **confirmed** · driver `C` · est. saved **96 MB**

`job_snapshots` is an append-only log/raw table with no retention policy. Growth here is never healthy data-model growth — it is storage + backup bloat and per-insert index overhead. Almost every larger Lovable DB is dominated by exactly one table like this.

**Evidence**
- _(sql)_ 96 MB, 76% of public schema

  ```
  SELECT relname, pg_total_relation_size(c.oid) FROM pg_class c ... ORDER BY 2 DESC;
  ```

**Remediation**
- Add retention to `job_snapshots`  _(sql_migration, review required)_

  ```sql
  -- keep a bounded window (tune interval); schedule as a daily job
  DELETE FROM job_snapshots WHERE created_at < now() - interval '30 days';
  VACUUM ANALYZE job_snapshots;
  -- if this is raw HTML/blobs: move payloads to Storage with a file_size_limit instead.
  ```

## Imported audit actions

_Context from the source audit. These actions are not counted in the waste-score._

| Rank | Policy | Severity | Status | Effort | Action |
|--:|---|---|---|---|---|
| 1 | POL-6 | Critical | Confirmed | XS | Delete reveal-service-key; rotate SERVICE_ROLE_KEY + CRON_SECRET |
| 2 | POL-4 | High | Confirmed | S | Fix broken model id in 7 fns -> catalog model; redeploy |
| 3 | POL-2 | High | Confirmed | S | Add retention/TTL to job_snapshots (keep latest+changed) |
| 4 | POL-7 | Medium | Confirmed | M | Batch monitoring_runs progress; upsert job_locations |
| 5 | POL-5 | High | Confirmed | S | Tighten job_snapshots RLS to service_role |

## Waste-score breakdown

| Policy | Findings | Points |
|---|--:|--:|
| POL-2 | 1 | 20 |

## Collector warnings

- index: pg_cron.active_jobs is not observed for `opportunity-monitor` (cron schema unreadable from app role)

---

_Generated by `recon scan` (read-only). Remediation is proposed, never applied — `recon clean --apply` (future) will require `confirmed` findings + review._
